<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="genius.mykhatamulquranbeta">

    <!-- To auto-c  omplete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-library android:name="genius.mykhatamulquranbeta.data.ApplicationConstants" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Splashscreen"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivityRegister" />
        <activity android:name=".startOfflineActivity" />
        <activity android:name=".KeteranganActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".startOnlineActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Grafik" />
        <activity android:name=".menuOfflineActivity" />
        <activity android:name=".tutorial" />
        <activity android:name=".profil.alifprofile" />
        <activity
            android:name=".profileFragment"
            android:label="@string/title_activity_profil_fragment"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>